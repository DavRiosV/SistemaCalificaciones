spring.application.name=sistemaCalificaciones

# Configuración de la base de datos
spring.datasource.url=jdbc:postgresql://localhost:5432/calificaciones_db
spring.datasource.username=postgres
spring.datasource.password=Nym3r1t0s.1108
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuración opcional de logs SQL
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE

# Configuración de JWT
jwt.secret=MiClaveSuperSeguraDeAlMenos32Caracteres
jwt.expiration=3600000

logging.level.root=INFO
logging.level.com.drvservicios.api=DEBUG

spring.security.user.name= # Deja vacío para deshabilitar
spring.security.user.password=

